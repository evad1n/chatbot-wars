import { Button } from '@material-ui/core';
import React from 'react';

export default function Finalize() {
    return (
        <div>
            <Button variant="contained" color="secondary">Create Bot</Button>
        </div>
    );
}
